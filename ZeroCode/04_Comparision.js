// There are many types of comparisions such as > , < , >= , <= , == , !=

/*

console.log("2">1);
console.log("02">1);
// Javascript automatically converts the values into numbers to comapare

*/

//console.log(null > 0);
//console.log(null == 0);
//console.log(null >=0);
//console.log(null == undefined); 
//also when we compare undefined with anyone we still get false it is only equal to null

// "===" This is called Strict check
// This prevents conversion so this do not convert a datatype into different one
