 const score = 400
 console.log(score); // Automaticallty assigns

 const balance =new Number(100) // specifically when we only want a number
 console.log(balance);
 
 console.log(balance.toString())
 console.log(balance.toFixed(3));


 const anotherOne = Number(23.567)

console.log(anotherOne.toPrecision(3)); //This is used to get precise values in a float number 

const hundred = 10000000
console.log(hundred.toLocaleString()); //It gives commans in numbers

//------------------------------------------------ Math -------------------------------------------------//

// .abs gives absolute value
// .around gives round figure also we have .celi and .floor
// we have .min and .max as well 
// Also we have .random it gives a random value between 0 and 1 by multiplying it with 10 it goes tp like 1 2 3 and if we do +1 we get a guranette integer


 